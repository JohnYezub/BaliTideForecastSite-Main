# Design source

`Landing Page.html` is the original design export from the Claude Design project
`ab91acd7-9dbc-418a-be3b-7c6a41580e85`, kept for reference/diffing.

Only the asset paths were rewritten to point at `../public/images/` and
`../src/styles/global.css` so it opens standalone from this folder. Markup and
copy are otherwise verbatim; `src/components/*.astro` are a 1:1 port of it.
